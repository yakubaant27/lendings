{
  // NOTE: Cloudflare Workers Builds overrides this with the dashboard
  // "Project name" (currently "lendings"), so the live URL is actually
  // https://lendings.yakuba-ant27.workers.dev — see sites/kuton66's
  // BookingForm workerUrl, which points at the real deployed name.
  "name": "kuton66-telegram-form",
  "main": "src/index.js",
  "compatibility_date": "2026-09-07"
}
